{% extends 'base.html.twig' %}

{% block body %}
    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_user_index') }}" class="btn btn-primary">Vissza a listára</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
