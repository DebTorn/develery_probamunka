{% extends 'base.html.twig' %}

{% block body %}

    {{ include('contact/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_contact_index') }}" class="btn btn-primary">Vissza a listára</a>

    {{ include('contact/_delete_form.html.twig') }}
{% endblock %}
